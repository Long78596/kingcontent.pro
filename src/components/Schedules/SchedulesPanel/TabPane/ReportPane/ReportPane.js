import React, { memo } from 'react';

const ReportPane = (props) => {
  return (
    <div className="bg-white">
      <h1 className="text-center text-6xl font-extrabold text-red-500">
        HELLO ĐÂY LÀ REPORT PANE
      </h1>
    </div>
  );
};

export default memo(ReportPane);
